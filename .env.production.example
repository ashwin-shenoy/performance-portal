# Production Environment Variables Template
# Copy this to .env.production and fill in with actual values
# NEVER commit .env.production to version control

# GitHub Container Registry
GITHUB_REPOSITORY=your-org/performance-portal
IMAGE_TAG=main

# Database Configuration
DB_USER=perfportal_user
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE

# Application Security
APP_USERNAME=admin
APP_PASSWORD=CHANGE_ME_STRONG_PASSWORD_32_CHARS_MIN
JWT_SECRET=CHANGE_ME_256_BIT_RANDOM_BASE64_STRING

# JPA Configuration
# Use 'validate' in production after initial deployment with 'update'
DDL_AUTO=validate

# CORS Origins (comma-separated)
CORS_ORIGINS=https://perf-portal.yourdomain.com,https://www.yourdomain.com

# Spring Profile
SPRING_PROFILE=production

# Optional: External monitoring
# SPRING_BOOT_ADMIN_URL=https://admin.yourdomain.com
# SPRING_BOOT_ADMIN_USERNAME=admin
# SPRING_BOOT_ADMIN_PASSWORD=password
